# Production environment variables
# Used when building with: npm run build

# Backend URL - Leave empty to auto-detect (uses window.location.origin)
# This ensures WebSocket uses HTTPS when page is served over HTTPS
VITE_API_URL=
